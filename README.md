Access frames at:
```https://r128w.github.io/bad-apple/bmp/[frame number].bmp```
or
```https://r128w.github.io/bad-apple/svg/[frame number].svg```

Frames are numbered as ```%d``` starting at 1.

For example, the third frame's BMP is at ```/bmp/3.bmp```.
